{
  "name": "ione",
  "version": "0.0.1",
  "private": true,
  "description": "ione — live AI math tutor (hackathon, Auth0 + Anthropic + Backboard tracks)",
  "type": "module",
  "scripts": {
    "test:ocr": "node scripts/test-ocr.mjs",
    "test:predictive": "node scripts/test-predictive.mjs",
    "seed-demo": "node scripts/seed-demo.mjs",
    "seed-demo:reset": "node scripts/seed-demo-reset.mjs",
    "eval": "cd api && RUN_EVAL=1 ./node_modules/.bin/vitest run tests/orchestrator.test.ts tests/kg --reporter=verbose",
    "eval:report": "node scripts/eval-report.mjs"
  },
  "dependencies": {
    "@types/three": "^0.184.0",
    "dotenv": "^16.4.7",
    "sharp": "^0.33.5",
    "three": "^0.184.0"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@supabase/supabase-js": "^2.104.1",
    "tslib": "^2.8.1"
  }
}
